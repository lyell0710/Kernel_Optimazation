# 怎么读这个项目(Kernel_Optimazation · 手写 CUDA 算子)

> **上游**:跨仓的先后次序见 `Resume/STUDY_MAP.md`(哪个仓先学、五条纵向主线);
> 本文只讲**进了这个仓之后怎么读**。
> **同级**:各仓根目录的同名文件结构一致,读熟一个就会读其余五个。
> **下游**:细节进 `docs/lectures/`,数字进 `records/`,输出进 `docs/talk/`。
>
> 本文讲三件事:**按什么顺序读十个算子、五层文档各在什么时候进、代码具体怎么读**。
> 记录制度不在此重复(见 /root/standards/CORE.md)。

---

## 总纲:这个仓不是用来"从头读一遍"的

十个算子、四篇深度讲义(约 2700 行)、六份 EXP 记录。从第一行读到最后一行是
**最低效的用法**,读完也不会写 kernel。

正确的用法是:**按机制依赖顺序走一条路径,每个算子只花 30-60 分钟,
在"讲不顺"的地方才展开对应的讲义。** 讲义是查的,不是通读的。

一条判据:**能不看代码在白纸上把某个算子的字节账写出来**,这个算子就算学会了;
写不出来,回去读的不是代码,是它的 project-intro 与版本梯 diff。

---

## 零、第一次进仓:按这个顺序打开(90 分钟)

不要打开任何讲义。按顺序做完这六步,你会有一条完整的主线,
之后所有细节都能挂上去。

| 步 | 打开什么 | 只看什么 | 时间 |
|---|---|---|---|
| 1 | `README.md` | 「性能结果」表 + 「关键发现」四段,**跳过代码导览** | 10 min |
| 2 | `PORTFOLIO.md` | 只看「方法论」一章的四节(profile 优先 / 控制变量 / 先判 bound / 全链路) | 15 min |
| 3 | `activation/project-intro.md` | 全文(最短、账最干净的算子) | 5 min |
| 4 | `activation/README.md` | 「性能结果」表 + 「关键发现」三段 | 10 min |
| 5 | 终端跑 `cd activation && python bench.py` | 看七条臂 × 四个区间的实际输出 | 15 min(含首编 60-90s) |
| 6 | `diff activation/src/silu_and_mul_v0.cu activation/src/silu_and_mul_v1.cu` | 对照第 3 步说的「融合上限 5/3」,看那一件事改在哪 | 15 min |

做完这六步,你应该能回答:**融合为什么能提速、上限是多少、实测对不对得上**。
对不上的话回第 3 步重读 project-intro 的版本梯表。

剩下 20 分钟做一次白板自测:不看代码,写出 `silu(gate)*up` 未融合与融合各搬几次字节,
推出 5/3,再说出实测 1.680 这个数说明了什么。

**写不出来就不要往下走。** 这条账是后面九个算子的地基。

---

## 零点五、之后每进一个新算子:固定五步(30-60 分钟)

| 步 | 动作 | 目的 |
|---|---|---|
| 1 | 读 `<算子>/project-intro.md` | 知道为什么做、每一级在验证什么假设 |
| 2 | 读 `<算子>/README.md` 的版本梯表 | 知道每一级实际拿到多少 |
| 3 | 读 `<算子>/src/*_vN.cu` 的**文件头注释** | 每级固定写:改了什么 / 字节账 / 跑前的预测 |
| 4 | `diff` 相邻两版 | 确认它真的只改了那一件事 |
| 5 | **预测与实测对不上的那一级** → `records/EXP-xxx` 的 §6 | 归因。这一步是全流程最值钱的 |

第 5 步没有对不上的话,这个算子 30 分钟就够了。
本仓有两处必须走第 5 步:fused-norm 的 v3/v4(预测大涨、**HBM 区间**实测 0%;
L2 常驻区间要到向量化修复后才拿到 v3 +21.3%、v4 +41.8%,见 EXP-K08《BF16x8 向量化未兑现的定位与修复》 §5)与
rope 的 v3→v4(变量隔离前后结论不同)。

**只有走到第 5 步卡住了,才去开对应的讲义**,而且只读讲义里对应的那一小节。

---

---

## 零点六、十个算子的完整打开顺序(精确到入口文件)

按机制依赖排,不是按开发时间。每一行走完上面的「固定五步」。
**没有 project-intro 的三个算子(标 ※),从它的 `README.md` 开头读起。**

| # | 算子 | 入口文件 | 走完能答的一个问题 | 卡住时开哪篇讲义 |
|---|---|---|---|---|
| 0 | activation | `activation/project-intro.md` | 融合的收益上限怎么算 | 讲义 03 §2.2 |
| 1 | cuda-reduce ※ | `cuda-reduce/README.md` | warp shuffle 凭什么比 smem 树快 | 讲义 03 §3.1 |
| 2 | softmax | `softmax/project-intro.md` | 为什么手写会有形状敏感性 | — |
| 3 | fused-norm | `fused-norm/project-intro.md` | **字节账为什么要在 HBM 层面记** | 讲义 03 §3.3 |
| 4 | rope | `rope/project-intro.md` | 就地更新为什么会读写冲突 | 讲义 03 §3.4 |
| 5 | gemv | `gemv/project-intro.md` | 为什么对照物也要跑 3 轮 | — |
| 6 | int8-quantize | `int8-quantize/project-intro.md` | 为什么 L2 命中率下降反而更快 | — |
| — | (读记录) | `records/EXP-K04_standard_library_baselines.md` | **怎么设计一个不骗自己的 benchmark** | — |
| 7 | gemm ※ | `gemm/README.md` | 为什么 occupancy 33% 反而最快 | 讲义 01 |
| 8 | flash-attn ※ | `flash-attn/README.md` | **wmma 的架构税具体是什么** | 讲义 02 |
| 9 | w8a8 | `w8a8/project-intro.md` | 为什么只插量化算子是负收益 | 讲义 04 |
| — | (接引擎) | `llm-engine/records/EXP-D24_w8a8_linear.md` | **算子级 2.1x 为什么变成一半赢一半输** | 引擎讲义 03 |

三个里程碑,走到就停下来做一次白板自测(标准见第四章):

- 走完 **#4**:阶段 1-2 的基本功齐了,访存型算子应该都能自己判 bound、算账。
- 走完 **EXP-K04**:测量方法论齐了。这是简历里所有数字的信用基础。
- 走完 **#9 + 接引擎**:能完整回答"有没有放到引擎里"那一类问题。

面试倒排的话,优先级是 **#3 → #8 → #9**(字节账 / 架构税 / 系统账),
这三个是背八股的人给不出的。

---

---

## 一、学习路径:六个阶段,每个阶段回答一个问题

顺序不是按开发时间,是按**机制依赖**——后面的算子复用前面的机制。

### 阶段 1 · 怎么把带宽吃满 → `cuda-reduce` → `softmax`

| 算子 | 引入的机制 | 关键数字 |
|---|---|---|
| reduce | 归约树 → warp shuffle → 循环展开;bank 冲突;线程发散 | HBM 区间 94.5% 峰值、与 CUB 贴平(v7 反快 0.1%);L2 区间 CUB 快 12.1%(EXP-K04) |
| softmax | 行归约 + 两遍扫;数值稳定(减 max);形状敏感性 | 对齐形状快 cuDNN 6.7%,非对齐慢 9.9% |

**学完要能答**:warp shuffle 凭什么比 smem 树快(同步次数从 log2(blockDim) 降到 1-2)?
bank 冲突怎么产生、`+1` 的 padding 为什么能消掉?
**为什么同一个算子在两个工作区间会得到相反的结论?**

最后一问是这个阶段真正的产出。它是后面所有测量的基础。

### 阶段 2 · 怎么少搬字节 → `fused-norm` → `activation` → `rope`

| 算子 | 引入的机制 | 关键数字 |
|---|---|---|
| fused-norm | 字节账 6→5→4;向量化;寄存器缓存 | v3/v4 在 HBM 区间**零收益**(v2 的 917 GB/s 已贴 91.0% 峰值);L2 常驻区间修复向量化后 v3 **+21.3%**、v4 **+41.8%** |
| activation | 最干净的融合验证 | 字节账预测 5/3=1.667,实测 **1.680** |
| rope | 就地更新的读写冲突;配对;launch 敏感区间 | q/k 合并 launch 在 HBM 区间 −1.2%、decode 区间 **1.39x** |

(本表数字取自三个算子各自的 `project-proof/data/derived_*_vec-after_stability.csv`,
即 EXP-K08《BF16x8 向量化未兑现的定位与修复》复采后的口径。)

**学完要能答**:字节账怎么算?**为什么必须在 HBM 层面记而不是指令层面**
(fused-norm v4 那一格的全部内容)?融合的收益上限是什么?
同一个改动为什么能在两个区间差 32 倍?

先读 `activation`(账最干净、预测最准),再读 `fused-norm`(预测被推翻的那个更有信息量),
最后 `rope`(结构最复杂)。

### 阶段 3 · 怎么知道自己真的赢了 → `gemv` → `int8-quantize` → 通读 EXP-K04

这一阶段不学新的 kernel 技巧,学**怎么设计一个不骗自己的 benchmark**。

- gemv:单轮测得领先 84%,让对照物同样跑 3 轮后回落到 34% ——
  **「vs X」的领先幅度,X 侧同样要 mean±std**。
- int8-quantize:v4 的 L2 命中率从 32% 跌到 2% 反而更快 ——
  **profiler 指标必须与时延趋势联读**,单看指标会得出反的结论。
- EXP-K04《标准库基准对照》:对照物必须同算子(reduce 对 CUB 不对 cublasSasum);
  **测量效度先算账**(等效带宽超过硬件峰值 = 数据落在 L2 而非 HBM)。

**学完要能答**:给你一个"我的 kernel 快 3 倍"的结论,你会问哪三个问题?

### 阶段 4 · 怎么把算力吃满 → `gemm`

```
v0 naive 5.2 → v1 smem tile 6.5(+25%) → v2 wmma 89.5(13.8x)
   → v3 cp.async 双缓冲 95.5 → v4 128x128 大 tile 133.1 TFLOPS(= 真 cuBLAS 85.6%)
   (85.6% 为主力机 CUDA 13.2 口径;CUDA 12.8 下为 77.9%)
```

**学完要能答**:为什么访存微调只是坡、指令世代才是台阶?
**为什么 occupancy 只有 33% 的那一版反而最快**(fragment 级 ILP + smem 复用喂满
Tensor Core,不靠线程数遮蔽延迟)?

配套讲义:`docs/lectures/01_tensorcore_gemm_ladder.md`。

### 阶段 5 · 为什么有的算子吃不满 → `flash-attn`

同一套 wmma 工具箱:GEMM 够到真 cuBLAS 的 **85.6%**(CUDA 13.2;12.8 下为 77.9%),FA2 只够到自家 Triton 的 **28%**。

**学完要能答**:wmma 的架构税具体是什么(accumulator fragment 的 lane→元素映射是
编译器私有的,行级 softmax 做不了,QK^T 结果必须落回 smem 再逐行重读,
每 tile 5 次 `__syncthreads` 的相位链)?为什么官方 FA2 用 CUTLASS/mma 而不是 wmma?

配套讲义:`docs/lectures/02_wmma_tax_fa2.md`。这是全仓最重要的一篇 ——
它是"什么时候手写 CUDA 会输"的定量答案。

### 阶段 6 · 算子级数字怎么变成系统级收益 → `w8a8` → llm-engine 接入

| 层次 | 数字 |
|---|---|
| 链路分解 | 量化 **1.7%** / INT8 GEMM 79.0% / 反量化 **26.7%** |
| 布局 | 一次 `.contiguous()` 让整条链路从 **2.167x** 变 **0.726x** |
| 存储层级陷阱 | 同一对 GEMV kernel 三个形状给出 4.43x / **8.69x(无效)** / **1.972x** |
| 接引擎(8B) | 显存 −36.9%、prefill −14.8%、decode **+10.7%(亏)** |
| decode 归因 | kernel 只占四成,**六成花在派发上** |

(前三行取自 `w8a8/project-proof/data/derived_w8a8_vec-after_stability.csv`;
后两行是 llm-engine 侧的接入口径,来源不同,不要混着引。)

**学完要能答**:为什么"只插量化算子"一定是负收益?
为什么 prefill 和 decode 的收益来源不是一回事?
**为什么算子级 2.1 倍到了引擎里会变成一半赢一半输?**

配套讲义:`docs/lectures/04_w8a8_kernels.md`(kernel 侧)与
llm-engine 的 `docs/lectures/03_quantization_deployment.md`(部署侧)。

### 路径全貌

```
阶段1 带宽    reduce → softmax
阶段2 字节账  activation → fused-norm → rope        ← 讲义 03
阶段3 测量    gemv → int8-quantize → EXP-K04
阶段4 算力    gemm                                   ← 讲义 01
阶段5 架构税  flash-attn                             ← 讲义 02
阶段6 系统    w8a8 → llm-engine 接入                 ← 讲义 04 + 引擎讲义 03
```

阶段 1-3 是基本功,阶段 4-5 是深度,阶段 6 是把前面全部串起来。
**面试的差异化几乎全在阶段 3、5、6** —— 那三段讲的是"你怎么知道自己没搞错"
和"你什么时候会输",背八股的人给不出。

---

## 二、文档怎么用:五层,按目的进

**不要从讲义开始。** 讲义单篇 400-1900 行,第一次读会淹,而且脱离代码看推导记不住。

| 你现在的目的 | 进哪一层 | 大概时间 |
|---|---|---|
| 知道这仓有什么、结论是什么 | 根 `README.md` 的**性能结果表 + 关键发现** | 10 min |
| 进入某一个算子 | 该算子的 `project-intro.md` | 5 min |
| 看某一级优化改了什么、拿到多少 | 该算子 `README.md` 的版本梯表 + 源码 diff | 20 min |
| 某个机制没想明白 | `docs/lectures/` 对应篇的**对应小节**(不要通读) | 20-60 min |
| 查某个数字怎么来的、可不可信 | `records/EXP-xxx` 的 §2 环境 / §5 结果 / **§7 异常与开放问题** | 10 min |
| 练面试怎么讲 | `docs/talk/`(讲稿 + 白板卡) | 15 min |
| 想知道当时为什么这么决定 | `LAB_JOURNAL.md` / `LEDGER.md` | 按需 |

三条使用纪律:

**1. `records` 的 §7 是最该读、最容易被跳过的一节。**
它写的是哪些数字不可信、哪些是推断而非实测、哪些协议有偏离。
面试官追问"这个数字有什么前提"时,答案在 §7 而不在 §5。
例:EXP-K05 的 §7 写明"第二次读被缓存接住"是从带宽上界**反推的推断**,
不是计数器实测 —— 这句话必须说,不说就是过度宣称。

**2. 讲义按小节查,不按篇读。** 每篇讲义的 §3(完整推导)与 §4(代码逐段走读)
是查的;§7(连环追问)是面试前一晚过的;§6(误区与边界)是**写简历前必看的**,
它列的就是不能说的话。

**3. 三层不能互相替代。** 讲义是学的、records 是验的、talk 是输出的。
只读讲义会记住结论但答不出"怎么测的";只读 records 会有数字但讲不出机制;
只背 talk 会在第二个追问就露馅。

---

## 三、代码怎么看:四条读法

### 读法一 · 版本梯用 `diff` 读,不要顺读

版本梯是按**每级只改一件事**设计的,所以 `diff` 就是那一件事,信噪比最高。
顺读会把 90% 的时间花在重复的样板上(指针计算、边界守卫、grid-stride 循环)。

```bash
cd /root/projects/Kernel_Optimazation
diff fused-norm/src/fused_norm_v1.cu fused-norm/src/fused_norm_v2.cu   # 归约方式
diff fused-norm/src/fused_norm_v2.cu fused-norm/src/fused_norm_v3.cu   # 向量化
diff rope/src/rope_v0.cu rope/src/rope_v1.cu                            # 配对消冲突
diff gemm/src/gemm_v1.cu  gemm/src/gemm_v2.cu                           # 换 wmma
```

配套动作(每级三步,约 5 分钟):

1. 读 `vN` 的**文件头注释** —— 每级固定写三样:**改了什么 / 字节账 / 跑前的预测**。
2. 看 `diff v(N-1) vN` —— 对照文件头说的"改了什么",确认它真的只改了这一件。
3. 回该算子 README 的版本梯表 —— 看这一级**实际**拿到多少。

**预测与实测对不上的那一级,是整条梯子最值钱的地方。** 去 `records` 的 §6 看归因。
本仓有两处典型:fused-norm 的 v3/v4(预测大涨、**HBM 区间**实测 0%;
L2 常驻区间要到向量化修复后才拿到 +21.3% / +41.8%,见 EXP-K08 §5)与
rope 的 v3→v4(预测 ±5%、实测 +2.1%,靠隔离变量才拿到)。

### 读法二 · 单个 kernel 从下往上读

不要从 `__global__` 的第一行往下读。正确顺序:

1. **先读文件底部的 launch 函数** —— `grid` / `block` 怎么配、有没有模板分支。
   这决定了"**一个线程负责什么**"。
2. **再读 kernel 里的循环边界与 stride** —— `for (int i = threadIdx.x; i < H; i += blockDim.x)`
   这一行告诉你数据怎么划分给线程。
3. **最后才读循环体** —— 在不知道"一个线程负责什么"之前,循环体是没有意义的。

以 `fused-norm/src/fused_norm_v4.cu` 为例:先看到底部按 `waves` 分派模板,
才明白 kernel 里那个 `MAX_WAVES` 的固定循环为什么必须是编译期常量。

### 读法三 · 读注释里的"改错会怎样",然后真的改坏它

本仓的注释都带这一句(这是写注释时的硬要求)。**读十遍不如改坏一次。**
挑三个改一遍,每个 10 分钟:

| 改哪里 | 怎么改 | 预期后果(注释里写了) |
|---|---|---|
| `fused-norm/src/fused_norm_v1.cu` | 删掉归约后那个 `__syncthreads()` | 结果随 blockDim 随机错,不崩不 NaN |
| `w8a8/include/w8a8.h` | `rintf` 改成 `truncf` | 量化误差不再零均值,逐层累积成 logits 偏移 |
| `llm-engine/src/w8a8.py` | 给 `wq.t()` 加一个 `.contiguous()` | 整条链路从 2.17x 掉到 0.73x |
| `rope/src/rope_v1.cu` | 改回"一线程一元素"就地写 | 结果错一半且随 block 调度随机变化 |

改完跑对应的 `bench.py` 看现象,再改回来。**"我见过它错成什么样"是面试里
最难伪造的一种熟悉度。**

### 读法四 · 先跑再读

每个子项目一条命令就出全表:

```bash
export CUDA_HOME=/usr/local/cuda
cd fused-norm && python bench.py     # 七/八条臂 × 四个工作区间
```

先看到数字,带着"为什么 v3 和 v2 一样快"这种具体疑问去读代码,
比无目的地通读高效一个量级。

老算子(reduce / softmax / gemv / int8-quantize / gemm / flash-attn)是独立 CMake:

```bash
cd gemm && mkdir -p build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && make -j
```

新算子(fused-norm / rope / activation / w8a8)走 torch 扩展 JIT,**首次编译 60-90 秒**,
之后走增量缓存。构建被中断会残留 lock 文件导致下次卡死 —— `bench_common.build_ext`
已自动清理。

---

## 四、自测与验收

每个阶段结束,**不看代码**在白纸上做下面这件事。做不出来 = 没学会,
回去重读的是 project-intro 与 diff,不是讲义。

| 阶段 | 验收动作 |
|---|---|
| 1 带宽 | 画出 warp shuffle 两级归约的数据流,说清同步次数怎么从 9 降到 2 |
| 2 字节账 | 写出 fused_add_rmsnorm 的四条访存,推出融合的加速上限,再说明为什么 v4 在 HBM 区间是 0%(而 L2 常驻区间修复向量化后不是) |
| 3 测量 | 给出一个 benchmark 设计清单(对照物同算子/同区间、3 轮、工作集算账) |
| 4 算力 | 画出 wmma 4×2 fragment 与 cp.async 双缓冲的主循环,解释 occupancy 33% 为什么最快 |
| 5 架构税 | 讲清 wmma 为什么做不了行级 softmax,以及那条 5 次同步的相位链 |
| 6 系统 | 讲清 W8A8 三步占比、布局值多少、decode 为什么亏、SmoothQuant 折进 norm 的理由 |

面试前一晚只做两件事:过一遍 `docs/talk/` 的一份讲稿(`fused_ops_talk.md`)与两张白板卡,
再过一遍四篇讲义的 §6(误区与边界)—— 那节列的就是**不能说的话**。

---

## 五、当前缺口(按补的价值排序)

1. **本仓缺 `docs/theory/` 速查层。** 姊妹仓 triton-kernels 有 7 篇(五节固定格式:
   一句话结论 / 机制 / 本项目实证 / 面试追问 Q&A / 延伸)。本仓只有 lectures(太长,
   查不动)与 talk(是讲稿,不是速查)。中间缺一层**一页纸、能在地铁上翻**的。
   优先补:bank 冲突、warp 归约、字节账、wmma 架构税、存储层级判定。
2. **`cuda-reduce` / `gemm` / `flash-attn` 缺 `project-intro.md`。**
   其余七个算子都有。它们恰好是路径里的阶段 1、4、5 —— 最需要背景叙事的三个。
3. **`softmax/` 下散着七份早期分析文档**,与现行口径存在两代并存的风险,
   宜按「过时即归档」移入 `docs/archive/`,只保留 README + project-intro。
